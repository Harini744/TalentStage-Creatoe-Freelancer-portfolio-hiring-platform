services:
  # ─────────────────────────────────────────
  # 1. BACKEND — Express + Prisma + SQLite
  # ─────────────────────────────────────────
  - type: web
    name: talentstage-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate && npx prisma migrate deploy && node prisma/seed.js
    startCommand: node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
      - key: DATABASE_URL
        value: file:./dev.db
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://talentstage-frontend.onrender.com
      - key: ANTHROPIC_API_KEY
        value: ""

  # ─────────────────────────────────────────
  # 2. FRONTEND — Vite + React (Static Site)
  # ─────────────────────────────────────────
  - type: web
    name: talentstage-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    plan: free
    envVars:
      - key: VITE_API_URL
        value: https://talentstage-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
